<div id="post-form">
  <%= simple_form_for @post, remote: true do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :content %>
    <%= f.text_area :content %>
  <%= f.submit(class: "btn btn-default") %>
</div>
